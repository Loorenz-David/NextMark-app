Porpose: 
- a store for vehicles
- holds selectors for obtaining an instance and instances
- the store  holds actions and selectors 

Rules:
- uses the StoreFactory.tsx to create the store.
- uses the entitySelectors.ts for abstracting selectors logic .
- uses ListStoreFactory.tsz to create the store for stats, query, pagination.















